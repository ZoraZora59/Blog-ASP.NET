@model IEnumerable<Blog_ASP.NET.Models.CommitList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TextID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Account)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CommitText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CommitChangeDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TextID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Account)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CommitText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CommitChangeDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CommitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CommitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CommitID })
        </td>
    </tr>
}

</table>
